{
  "name": "nappyhood-mis",
  "version": "1.0.0",
  "description": "Nappyhood Salon Management System",
  "scripts": {
    "build": "cd backend && npm ci && npm run build && npx prisma generate",
    "start": "cd backend && npx prisma migrate deploy && (npm run seed || echo 'Seeding failed, continuing...') && npm start"
  },
  "engines": {
    "node": "20.x"
  }
}